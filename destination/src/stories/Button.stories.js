import React, { Children } from "react";
import Button from "../utils/button.component";

export default {
  title: "Destination/Button",
  component: Button,
};

export const Primary = () => <button variant="primary"> Primary </button>;
