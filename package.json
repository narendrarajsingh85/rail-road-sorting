{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.0.0",
    "husky": "^8.0.0"
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "prepare": "husky install"
  }
}
